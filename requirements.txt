# CPU-side dependencies for the benchmark + instancer code in src/.
# GPU work (v4b inference, nnU-Net training) runs on tulen in its own envs
# (~/dinov3_env, ~/nnunet_env) and is NOT covered by this file.
numpy>=1.24
scipy>=1.11
scikit-image>=0.22
networkx>=3.2
h5py>=3.10
tifffile>=2024.1.30
matplotlib>=3.8
optuna>=3.5
pytest>=8.0
