# Already satisfied inside the spheroseg-ml container -- install only for a standalone env.
# Versions are the ones this package was verified against; the container's are compatible.
torch>=2.0
numpy>=1.24,<2
scipy>=1.10
scikit-image>=0.20
networkx>=3.0
tifffile>=2023.1.1
Pillow>=9.0
matplotlib>=3.6      # only for --overlay
